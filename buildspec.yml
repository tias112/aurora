version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.10
  build:
    commands:
      - echo Deploying Cloudformation Stack
      - aws cloudformation deploy --template-file s3bucket.yml --stack-name my-s3-bucket-stack --capabilities CAPABILITY_IAM